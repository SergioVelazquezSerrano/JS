var docObj = document.doctype;

console.log(
    "doctypeObj.name: " + docObj.name + "\n" +
    "doctypeObj.internalSubser: " + docObj.internalSubset + "\n" +
    "doctypeObj.publicId: " + docObj.publicId + "\n" +
    "doctypeObj.systemId:" + docObj.systemId + "\n" 
);